# Bouncing Ball
To build and run the bouncing ball thing, execute

make sim
./pba/pbalitesim

Then press "u" or "U" to see the usage of the program. 

to clean the project, run

make cleanall

make clean could leave around some .o files that are troublesome to rebuild between unit tests.

Unit tests can also be built and executed with
make test